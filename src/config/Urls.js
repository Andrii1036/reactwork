const baseUrl = 'https://jsonplaceholder.typicode.com';


export const endpoint = {
    users: '/users',
    posts: '/posts',
    comments: '/comments',
    albums: '/albums',
    photos: '/photos'
};

export default baseUrl;
